# Claude Code Workspace Instructions

Read and follow `AGENTS.md` as the canonical agency-wide workspace guide. Also follow the path-scoped rules in `.claude/rules/`.

When working inside a client or team folder, read that folder's `CLAUDE.md` and `AGENTS.md` if present. Keep those two files semantically aligned: `AGENTS.md` is the Codex entry point and `CLAUDE.md` is the Claude Code entry point.
