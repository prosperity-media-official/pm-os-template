---
paths:
  - "team/sales/**"
---

# Sales rules

Pre-engagement emails, proposals, lead notes, and call preparation live in `team/sales/` and use a `YYYY-MM-DD-<lead>-<artifact>.md` filename. Once a lead becomes a client, route new work to `clients/<client>/` and migrate relevant source material deliberately.
