# Example Client — Claude Code Instructions

Read and follow this folder's `AGENTS.md` plus the workspace-root `CLAUDE.md`. The client-specific facts and constraints live in `AGENTS.md` and the four mandatory files under `business/`.
